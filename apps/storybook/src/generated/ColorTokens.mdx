import { Meta } from "@storybook/addon-docs/blocks";
import { DesignTokenDocBlock } from "storybook-design-token";

<Meta title="Foundations/Color Tokens" />

# Color Tokens

Complete structural color documentation generated from the full Figma token export. This page covers theme, semantic, primitive, and utility color tokens.

## Theme / Cars24

Cars24 theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Cars24"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Theme / Team BHP

Team BHP theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Team BHP"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Theme / CarInfo

CarInfo theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / CarInfo"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Theme / Vehicle Info

Vehicle Info theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Vehicle Info"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Background

Background fills and overlays across surface, brand, and feedback states.

<DesignTokenDocBlock
  categoryName="Semantic / Background"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Text

Readable foreground text colors for default, inverse, brand, and feedback usage.

<DesignTokenDocBlock
  categoryName="Semantic / Text"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Icon

Icon foreground colors aligned with the semantic text and feedback system.

<DesignTokenDocBlock
  categoryName="Semantic / Icon"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Border

Border and outline colors for default, brand, inverse, and status surfaces.

<DesignTokenDocBlock
  categoryName="Semantic / Border"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Base

Raw foundational black and white primitives used as palette anchors.

<DesignTokenDocBlock
  categoryName="Primitive / Base"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Amber

Raw amber palette ramp from lightest tint to strongest tone.

<DesignTokenDocBlock
  categoryName="Primitive / Amber"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Bright Blue

Raw bright blue palette ramp for informational and accent building blocks.

<DesignTokenDocBlock
  categoryName="Primitive / Bright Blue"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Cobalt Blue

Raw cobalt blue palette ramp for saturated brand-adjacent usage.

<DesignTokenDocBlock
  categoryName="Primitive / Cobalt Blue"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Cool Mint

Raw cool mint palette ramp for fresh green-blue tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Cool Mint"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Cream Beige

Raw cream beige palette ramp for warm neutral tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Cream Beige"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Crimson Red

Raw crimson red palette ramp for strong red tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Crimson Red"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Drive Pink

Raw drive pink palette ramp for vivid magenta tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Drive Pink"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Electric Violet

Raw electric violet palette ramp for vibrant purple tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Electric Violet"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Green

Raw green palette ramp for success and natural tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Green"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Lotus Blue

Raw lotus blue palette ramp for softer blue tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Lotus Blue"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Mint Green

Raw mint green palette ramp for light green tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Mint Green"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Neutral

Raw neutral grayscale palette ramp for non-brand tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Neutral"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Orange

Raw orange palette ramp for warning and warm accent tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Orange"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Pop Purple

Raw pop purple palette ramp for expressive purple tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Pop Purple"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Red

Raw red palette ramp for danger and alert tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Red"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Sky Surge

Raw sky surge palette ramp for energetic blue tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Sky Surge"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Slate

Raw slate palette ramp for cool neutral tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Slate"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Primitive / Teal

Raw teal palette ramp for blue-green tonal steps.

<DesignTokenDocBlock
  categoryName="Primitive / Teal"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Utility / Alpha

Alpha overlays for black and white transparency steps used across surfaces and states.

<DesignTokenDocBlock
  categoryName="Utility / Alpha"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Utility / Service

Service-specific utility ramps for buy, sell, insurance, loan, and related service surfaces.

<DesignTokenDocBlock
  categoryName="Utility / Service"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>
