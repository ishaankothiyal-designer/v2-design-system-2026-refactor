import { Meta } from "@storybook/addon-docs/blocks";
import { DesignTokenDocBlock } from "storybook-design-token";

<Meta title="Foundations/Color Tokens" />

# Color Tokens

Complete structural color documentation generated from the full Figma token export. This page covers every exported color token across theme, semantic, primitive, and utility families.

## Theme / Cars24

Cars24 theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Cars24"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Theme / Team BHP

Team BHP theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Team BHP"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Theme / CarInfo

CarInfo theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / CarInfo"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Theme / Vehicle Info

Vehicle Info theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Vehicle Info"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Semantic / Background

Background fills and overlays across surface, brand, and feedback states.

<DesignTokenDocBlock
  categoryName="Semantic / Background"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Semantic / Text

Readable foreground text colors for default, inverse, brand, and feedback usage.

<DesignTokenDocBlock
  categoryName="Semantic / Text"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Semantic / Icon

Icon foreground colors aligned with the semantic text and feedback system.

<DesignTokenDocBlock
  categoryName="Semantic / Icon"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Semantic / Border

Border and outline colors for default, brand, inverse, and status surfaces.

<DesignTokenDocBlock
  categoryName="Semantic / Border"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Amber

Primitive Amber palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Amber"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Base

Primitive Base palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Base"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Bright Blue

Primitive Bright Blue palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Bright Blue"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Cobalt Blue

Primitive Cobalt Blue palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Cobalt Blue"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Cool Mint

Primitive Cool Mint palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Cool Mint"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Cream Beige

Primitive Cream Beige palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Cream Beige"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Crimson Red

Primitive Crimson Red palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Crimson Red"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Drive Pink

Primitive Drive Pink palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Drive Pink"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Electric Violet

Primitive Electric Violet palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Electric Violet"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Green

Primitive Green palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Green"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Lotus Blue

Primitive Lotus Blue palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Lotus Blue"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Mint Green

Primitive Mint Green palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Mint Green"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Neutral

Primitive Neutral palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Neutral"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Orange

Primitive Orange palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Orange"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Pop Purple

Primitive Pop Purple palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Pop Purple"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Red

Primitive Red palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Red"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Sky Surge

Primitive Sky Surge palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Sky Surge"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Slate

Primitive Slate palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Slate"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Primitive / Teal

Primitive Teal palette exported from the shared base color system.

<DesignTokenDocBlock
  categoryName="Primitive / Teal"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Alpha / Black

Utility Alpha / Black colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Alpha / Black"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Alpha / White

Utility Alpha / White colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Alpha / White"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Buy Car

Utility Service / Buy Car colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Buy Car"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Car Check

Utility Service / Car Check colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Car Check"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Challan

Utility Service / Challan colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Challan"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Garage

Utility Service / Garage colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Garage"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Insurance

Utility Service / Insurance colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Insurance"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Loan

Utility Service / Loan colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Loan"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>

## Utility / Service / Sell Car

Utility Service / Sell Car colors for alpha overlays and service-specific accents.

<DesignTokenDocBlock
  categoryName="Utility / Service / Sell Car"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="table"
/>
