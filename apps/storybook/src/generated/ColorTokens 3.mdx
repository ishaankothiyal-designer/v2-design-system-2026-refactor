import { Meta } from "@storybook/addon-docs/blocks";
import { DesignTokenDocBlock } from "storybook-design-token";

<Meta title="Foundations/Color Tokens" />

# Color Tokens

Complete structural color documentation generated from the full Figma token export. This page covers every exported color token across theme modes and semantic token families.

## Theme / Cars24

Cars24 theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Cars24"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Theme / Team BHP

Team BHP theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Team BHP"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Theme / CarInfo

CarInfo theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / CarInfo"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Theme / Vehicle Info

Vehicle Info theme palette with brand and alternate brand ramps from the Figma token export.

<DesignTokenDocBlock
  categoryName="Theme / Vehicle Info"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Background

Background fills and overlays across surface, brand, and feedback states.

<DesignTokenDocBlock
  categoryName="Semantic / Background"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Text

Readable foreground text colors for default, inverse, brand, and feedback usage.

<DesignTokenDocBlock
  categoryName="Semantic / Text"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Icon

Icon foreground colors aligned with the semantic text and feedback system.

<DesignTokenDocBlock
  categoryName="Semantic / Icon"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>

## Semantic / Border

Border and outline colors for default, brand, inverse, and status surfaces.

<DesignTokenDocBlock
  categoryName="Semantic / Border"
  maxHeight={720}
  pageSize={Number.MAX_VALUE}
  showSearch={false}
  viewType="card"
/>
