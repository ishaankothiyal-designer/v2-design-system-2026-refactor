import { Meta } from "@storybook/addon-docs/blocks";
import { CarBrandLogoLibraryGallery } from "./CarBrandLogoLibraryGallery";
import { totalCarBrandImageCount, totalCarBrandVectorCount } from "./carBrandLogoLibraryData";

<Meta title="Foundations/Car Brand Logo Library" />

# Car Brand Logo Library

Complete car brand logo documentation sourced from the asset library Figma file. This page mirrors the icon library pattern, with fast filters for logo type and container size so image and vector exports stay easy to scan.

The current library contains **{totalCarBrandImageCount} raster image logos** and **{totalCarBrandVectorCount} vector logos**. Use the controls below to switch between `image` and `vector`, then preview each asset in either the `4:3` or `1:1` container preset.

<CarBrandLogoLibraryGallery />
